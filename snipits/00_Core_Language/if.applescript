-- if statements
(*
set z to "bar"
if (z ­ "foo") then display dialog "z is not foo"

set x to "foo"
if (x = "foo") then
	display dialog "x is foo"
end if

set y to "bar"
if (y = "foo") then
	display dialog "y is foo"
else
	display dialog "y is bar"
end if
*)